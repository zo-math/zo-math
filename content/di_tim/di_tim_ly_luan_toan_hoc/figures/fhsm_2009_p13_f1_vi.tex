\documentclass[tikz,border=6pt]{standalone}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,calc}

\begin{document}
\begin{tikzpicture}[
  font=\sffamily,
  box/.style={draw, fill=white, minimum width=4.8cm, minimum height=1.35cm, align=center},
  arr/.style={-Latex, line width=1.2pt},
  lab/.style={font=\sffamily\bfseries},
  every node/.style={inner sep=6pt}
]

% --- Background + outer frame (like the sample) ---
\fill[black!10] (-1.2,-1.2) rectangle (12.1,6.5);
\draw[black, line width=0.9pt] (-1.2,-1.2) rectangle (12.1,6.5);

% --- Nodes (2×2) ---
\node[box] (rws)   at (1.5,5) {Tình huống trong\\thế giới thực};

\node[box] (rsol)  at (9.4,5) {Giải pháp trong\\thế giới thực};

\node[box, minimum height=1.55cm] (mm)  at (1.5,0) {Mô hình toán học,\\bao gồm các giả định};

\node[box] (mc) at (9.4,0) {Các kết luận\\toán học};

% --- Arrows + labels ---
% Top: right -> left
\draw[arr] (rsol.west) -- (rws.east)
  node[midway, above=6pt, lab, align=center] {Kiểm tra kết quả; \\Lặp lại quy trình};

% Left vertical: top -> bottom
\draw[arr] (rws.south) -- (mm.north);
\node[lab, rotate=90] at ($(rws.south)!0.5!(mm.north) + (-0.5,0)$) {Xây dựng mô hình};

% Bottom: left -> right
\draw[arr] (mm.east) -- (mc.west)
  node[midway, above=6pt, lab, align=center] {Xác định kết quả};

% Right vertical: bottom -> top
\draw[arr] (mc.north) -- (rsol.south);
\node[lab, rotate=90] at ($(mc.north)!0.5!(rsol.south) + (0.5,0)$) {Diễn giải};

\end{tikzpicture}
\end{document}

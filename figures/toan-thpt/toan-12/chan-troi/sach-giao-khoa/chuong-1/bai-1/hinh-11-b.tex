\documentclass[border=1mm]{standalone}
\newcommand{\FontPath}{../../../../../../../assets/fonts/} 
\input{../../../../../../../assets/tex/tikz-preamble.tex}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[
    axis lines = middle,    
    axis line style={<->},
    xlabel = {$x$},
    ylabel = {$y$},
    xlabel style={at={(axis cs:4,0)},anchor=west},
    ylabel style={at={(axis cs:0,5)},anchor=south},
    xtick = {-3, -1, 0, 1, 3},
    ytick = {-2, -1, 0, 3, 4},
    ymin = -3, ymax = 5,
    xmin = -4, xmax = 4,
    scale only axis,
  ]

    % Đường cong mượt đi qua các điểm
    \addplot[
      smooth,
      thick
    ] coordinates {
      (-3, -2)
      (-2.6,0)
      (-2,1.6)
      (-1, 3)
      (-.35,1.225)   
      (0, 1)
      (.275,.85)
      (.6,0)
      (.9,-.9)
      (1, -1)
    };

    % Đoạn thẳng tiếp nối
    \addplot[
      thick
    ] coordinates {
      (1, -1)
      (3, 4)
    };

    % Các đường kẻ phụ
    \draw [dashed, opacity=.5] (-3,0) -- (-3,-2) -- (0,-2);
    \draw [dashed, opacity=.5] (-1,0) -- (-1,3) -- (0,3);
    \draw [dashed, opacity=.5] (1,0) -- (1,-1) -- (0,-1);
    \draw [dashed, opacity=.5] (3,0) -- (3,4) -- (0,4);

  \end{axis}
\end{tikzpicture}
\end{document}

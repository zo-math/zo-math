\documentclass[border=6pt]{standalone}
\usepackage{pgfplots}
\usepackage{unicode-math}
% \setmathfont{STIX Two Math}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
    axis lines=middle,
    % axis line style={<->},   % hai đầu mũi tên
    axis line style={<->, very thin, gray!60}, % trục nhẹ
    xmin=-3.5, xmax=3.5,
    ymin=-1, ymax=2,
    xlabel={$x$}, ylabel={$y$},
    xlabel style={right},
    ylabel style={above},
    ticks=none,
    enlargelimits=false,
    scale only axis,                % chỉ co giãn phần đồ thị, không kéo khung ngoài
]

% Đồ thị hàm y = 1
\addplot[domain=-3.5:3.5, samples=2] {1};
% \addplot[thick, black] {1}; 


% Ghi điểm gốc tọa độ O
\node[below left] at (axis cs:0,0) {$O$};

% Ghi giá trị 1 trên trục tung
\node[anchor=west] at (axis cs:0,1) [above left] {$1$};

\end{axis}
\end{tikzpicture}
\end{document}

\documentclass[border=6pt]{standalone}
\usepackage{pgfplots}
\usepackage{unicode-math}
% \setmathfont{STIX Two Math}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
    axis lines=middle,
    axis line style={<->, very thin, gray!60}, 
    xmin=-2, xmax=2,
    ymin=-2, ymax=2,
    xlabel={$x$}, ylabel={$y$},
    xlabel style={right},
    ylabel style={above},
    ticks=none,
    enlargelimits=false,
    scale only axis, % chỉ co giãn phần đồ thị, không kéo khung ngoài
    axis equal, % tỉ lệ hai trục như nhau
]

% Đồ thị hàm y = 1
\addplot[domain=-2:2, samples=2] {x};
% \addplot[thick, black] {1}; 
\draw [dashed, very thin, gray!60] (1,0)--(1,1)--(0,1);


% Ghi điểm gốc tọa độ O
\node[above left] at (axis cs:0,0) {$O$};

% Ghi giá trị 1 trên trục tung
\node[anchor=west] at (axis cs:0,1) [above left] {$1$};
\node[anchor=west] at (axis cs:1,0) [below] {$1$};

\end{axis}
\end{tikzpicture}
\end{document}
